
.pull-right
  = link_to new_dipendente_path, class: "btn btn-primary" do
    %i.icon-plus
    Nuovo scagnozzo

%h2 Tutti gli scagnozzi


%table.table
  %tr
    %th Ruolo
    %th Nome
    %th


  - @dipendenti.each do |dipendente|
    %tr
      %td= link_to dipendente.ruolo, dipendente.ruolo
      %td= link_to dipendente.nome, dipendente
      %td
        .pull-right
          = link_to edit_dipendente_path(dipendente), class: "btn btn-success" do
            %i.icon-pencil
          = link_to dipendente, :confirm => 'Are you sure?', :method => :delete, class: "btn btn-danger" do
            %i.icon-minus



